version: '3.4'

services:

  nextjs-frontend:
    build:
      dockerfile: frontend.dockerfile
    restart: always
    env_file:
      - .env
    expose:
      - "3000"
    networks:
      - backend_default 

networks:
  backend_default:
    external: true
